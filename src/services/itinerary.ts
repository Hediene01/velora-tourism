import type { BuilderState, ItineraryDay, PaymentEstimate } from '@/types';
const names:Record<string,string>={istanbul:'Istanbul',cappadocia:'Cappadocia',aegean:'Aegean Coast','black-sea':'Black Sea',sapanca:'Sapanca'};
export function tripNights(s:BuilderState){if(!s.arrival||!s.departure)return 0;return Math.max(0,Math.ceil((new Date(s.departure).getTime()-new Date(s.arrival).getTime())/86400000));}
export function estimateTrip(s:BuilderState):PaymentEstimate{const nights=Math.max(tripNights(s),3);const travelers=s.adults+s.children;const serviceCost=s.services.length*110;const style=s.style==='luxury'?1.45:1;const base=Math.round((nights*210+travelers*95+serviceCost)*style/10)*10;return{currency:'EUR',low:base,high:Math.round(base*1.28/10)*10,finalConfirmed:false};}
export function generateItinerary(s:BuilderState):ItineraryDay[]{const nights=Math.max(1,tripNights(s));const selected=s.destinations.length?s.destinations:['istanbul'];return Array.from({length:Math.min(nights,9)},(_,i)=>{const key=selected[Math.min(Math.floor(i/Math.max(1,Math.ceil(nights/selected.length))),selected.length-1)];const destination=names[key]??key;return{id:`day-${i+1}`,day:i+1,destination,morning:i===0?'Private welcome and orientation':'Curated neighborhood discovery',afternoon:key==='istanbul'?'Bosphorus and waterfront exploration':'Private scenic experience',evening:'A considered dining suggestion at your own pace',transfer:i===0?'Private airport meet-and-greet':'Private vehicle on request',duration:'7–8 hours',included:s.services.slice(0,3),optionalUpgrades:['Private photographer','Signature dining reservation'],estimatedPrice:220+s.services.length*35};});}
